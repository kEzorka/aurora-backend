"""Каноническая форма из docs/DATA_CONTRACT.md §1 и docs/DOMAIN.md §4–5.

Один источник правды для адаптеров, валидаторов, хранилища и API. Числа здесь
не «настройки», а контракт: поменять их — значит поменять формат хранилища
и сломать всё, что уже записано.

Модуль намеренно зависит только от numpy. Он не знает ни про xarray, ни про
Zarr, ни про HTTP — иначе им нельзя пользоваться отовсюду.
"""

from collections.abc import Mapping
from types import MappingProxyType
from typing import Final, NamedTuple

import numpy as np

GRID_STEP: Final = 0.25

# Широта строго убывает, долгота строго возрастает — docs/DATA_CONTRACT.md §1.
# Округление до сотых обязательно: arange на float даёт 55.749999999999996,
# и сравнение узла сетки с запросом пользователя перестаёт работать.
LAT: Final[np.ndarray] = np.round(np.arange(90.0, -90.0 - GRID_STEP / 2, -GRID_STEP), 2)
LON: Final[np.ndarray] = np.round(np.arange(-180.0, 180.0 - GRID_STEP / 2, GRID_STEP), 2)
GRID_SHAPE: Final = (int(LAT.size), int(LON.size))
GRID_POINTS: Final = GRID_SHAPE[0] * GRID_SHAPE[1]
GRID_NAME: Final = "0.25deg-global"

# Уровни давления в гПа. Порядок фиксированный и проверяется поэлементно:
# Aurora принимает поля именно в этом порядке (docs/DOMAIN.md §5).
PRESSURE_LEVELS: Final = (50, 100, 150, 200, 250, 300, 400, 500, 600, 700, 850, 925, 1000)

# Приземные переменные Aurora 1.5 (ADDENDUM-01 §1). Их 26 против четырёх у
# `aurora-0.25-finetuned`, и делятся они на три неравные части, которые нельзя
# складывать в один кортеж и забывать про разницу:
#
#   * 18 входных — их обязан достать адаптер из GRIB;
#   * `insolation` — тоже вход, но модель считает его сама из `valid_time` и
#     геометрии, поэтому его не качают и не хранят;
#   * 7 «только на выход» — модель их предсказывает, но на вход не требует
#     (при авторегрессии сама подставляет нули).
SURFACE_INGESTED_VARS: Final = (
    "2t",
    "10u",
    "10v",
    "msl",
    "2d",
    "tcwv",
    "tcc",
    "100u",
    "100v",
    "sp",
    "lcc",
    "mcc",
    "hcc",
    "skt",
    "stl1",
    "swvl1",
    "ci",
    "sd",
)
PRESCRIBED_VARS: Final = ("insolation",)
SURFACE_INPUT_VARS: Final = SURFACE_INGESTED_VARS + PRESCRIBED_VARS

# Суффикс `_1h` не украшение: у Aurora 1.5 эти поля накоплены за **один час**
# перед сроком, а не за шаг прогноза. Назови их `tp` и `sf` — и сумма за сутки
# посчиталась бы как 4 × 6-часовых накоплений, то есть завышена в шесть раз.
SURFACE_OUTPUT_ONLY_VARS: Final = (
    "i10fg",
    "blh",
    "uvb_1h",
    "ssrd_1h",
    "ttr_1h",
    "tp_1h",
    "sf_1h",
)
SURFACE_VARS: Final = SURFACE_INPUT_VARS + SURFACE_OUTPUT_ONLY_VARS

#: Что реально ложится в Zarr после прогона: `insolation` восстанавливается из
#: времени за наносекунды, хранить его — 3 ГБ на прогноз ни за что.
SURFACE_STORED_VARS: Final = SURFACE_INGESTED_VARS + SURFACE_OUTPUT_ONLY_VARS

ATMOS_VARS: Final = ("t", "u", "v", "q", "z")

# Имена канона физические; у Aurora часть полей названа по способу нормировки.
# Префикс `scaled_` означает лог-преобразование **внутри модели**: на вход и
# на выход идут обычные метры, поэтому переименование — единственное, что
# нужно сделать при сборке батча (как и с `z_surf`).
AURORA_SURFACE_NAMES: Final[dict[str, str]] = {
    "sd": "scaled_sd",
    "tp_1h": "scaled_tp_1h",
    "sf_1h": "scaled_sf_1h",
}

# docs/DOMAIN.md §5 называет статический геопотенциал `z` — так же, как
# геопотенциал на уровнях давления. У Aurora это разные словари, и коллизии
# там нет; в одном плоском Dataset два `z` существовать не могут, и проверка
# «все поля на месте» перестаёт видеть отсутствие орографии. Поэтому в каноне
# статический называется `z_surf`, а обратно в `z` его переименовывает только
# сборка батча для модели — единственное место, где это имя нужно.
#
# Aurora 1.5 требует 36 статических полей (растительность, тип почвы,
# орография, подсеточный рельеф). Мы их не собираем: они приходят готовым
# файлом вместе с чекпоинтом
# (huggingface.co/microsoft/aurora, `aurora-0.25-v1.5-static.pickle`).
# Здесь перечислено то, что качаем сами — для масок суши в API и для
# резервного чекпоинта `aurora-0.25-finetuned`. Имена наши, не апстримовые:
# у Aurora 1.5 те же величины зовутся `z`, `slt_0..slt_7` и приходят из
# pickle. Скармливать эти три модели нельзя — она ждёт все 36.
STATIC_VARS: Final = ("lsm", "z_surf", "slt")
AURORA_STATIC_NAMES: Final[dict[str, str]] = {"lsm": "lsm", "z_surf": "z", "slt": "slt"}

#: Контрактное число из ADDENDUM-01 §3: 26 приземных + 5 × 13 на уровнях = 91.
#: Это размер шага **модели**.
FIELDS_PER_STEP: Final = len(SURFACE_VARS) + len(ATMOS_VARS) * len(PRESSURE_LEVELS)

#: А это размер шага **на диске**: 91 минус `insolation`. Объёмы считать по нему.
STORED_FIELDS_PER_STEP: Final = len(SURFACE_STORED_VARS) + len(ATMOS_VARS) * len(PRESSURE_LEVELS)

# СИ, docs/DOMAIN.md §4. Перевод в °C, гПа и мм — задача слоя API,
# а не хранилища: константа 273.15 в storage/ или в adapters/ — симптом.
UNITS: Final[dict[str, str]] = {
    "2t": "K",
    "10u": "m s-1",
    "10v": "m s-1",
    "msl": "Pa",
    "2d": "K",
    "tcwv": "kg m-2",
    "tcc": "1",
    "100u": "m s-1",
    "100v": "m s-1",
    "sp": "Pa",
    "lcc": "1",
    "mcc": "1",
    "hcc": "1",
    "skt": "K",
    "stl1": "K",
    "swvl1": "m3 m-3",
    "ci": "1",
    # Водный эквивалент, а не высота снежного покрова: 1 м здесь — это метр
    # воды, то есть несколько метров снега.
    "sd": "m",
    "insolation": "W m-2",
    "i10fg": "m s-1",
    "blh": "m",
    # Радиация накоплена за час, поэтому джоули на метр, а не ватты.
    "uvb_1h": "J m-2",
    "ssrd_1h": "J m-2",
    "ttr_1h": "J m-2",
    "tp_1h": "m",
    "sf_1h": "m",
    "t": "K",
    "u": "m s-1",
    "v": "m s-1",
    "q": "kg kg-1",
    "z": "m2 s-2",
    "z_surf": "m2 s-2",
    "lsm": "1",
    "slt": "1",
    "tp": "m",
}

# docs/DATA_CONTRACT.md §4, уровень «физика». Границы включительные.
# Диапазоны на уровнях давления шире приземных: на 50 гПа ветер и температура
# ведут себя не так, как на 10 метрах.
PHYSICAL_RANGES: Final[dict[str, tuple[float, float]]] = {
    "2t": (180.0, 340.0),
    "t": (150.0, 350.0),
    "msl": (87_000.0, 108_500.0),
    "2d": (150.0, 320.0),
    "tcwv": (0.0, 120.0),
    "tcc": (0.0, 1.0),
    "lcc": (0.0, 1.0),
    "mcc": (0.0, 1.0),
    "hcc": (0.0, 1.0),
    "ci": (0.0, 1.0),
    "100u": (-150.0, 150.0),
    "100v": (-150.0, 150.0),
    # Приземное давление, в отличие от приведённого к уровню моря, честно
    # падает на высоте: на Эвересте ~33 кПа. Один диапазон на `sp` и `msl`
    # означал бы, что орографию можно перепутать местами и не заметить.
    "sp": (30_000.0, 110_000.0),
    "skt": (170.0, 355.0),
    "stl1": (200.0, 350.0),
    "swvl1": (0.0, 1.0),
    "sd": (0.0, 20.0),
    "insolation": (0.0, 1_500.0),
    "i10fg": (0.0, 150.0),
    "blh": (0.0, 10_000.0),
    # Верхняя граница — солнечная постоянная за час: 1361 × 3600 ≈ 4.9e6.
    "ssrd_1h": (0.0, 5.0e6),
    "uvb_1h": (0.0, 5.0e5),
    # Уходящее излучение на верхней границе атмосферы всегда отрицательное:
    # поток направлен наружу. Положительное значение — перепутанный знак.
    "ttr_1h": (-2.0e6, 0.0),
    # Мировой рекорд часовых осадков — около 0.3 м.
    "tp_1h": (0.0, 0.5),
    "sf_1h": (0.0, 0.2),
    "10u": (-120.0, 120.0),
    "10v": (-120.0, 120.0),
    "u": (-150.0, 150.0),
    "v": (-150.0, 150.0),
    "q": (0.0, 0.05),
    # Геопотенциал на уровнях давления и геопотенциал поверхности отличаются
    # на порядок: 50 гПа — это ~20 км, то есть ~200 000 м²/с², а Эверест —
    # 86 800. Один диапазон на два поля означал бы, что орография, записанная
    # вместо уровня 50 гПа, проходит проверку.
    "z": (-15_000.0, 350_000.0),
    "z_surf": (-6_000.0, 90_000.0),
    "lsm": (0.0, 1.0),
    "slt": (0.0, 7.0),
    # Осадки за интервал, не накопление от начала прогона. Отрицательное
    # значение — верный признак, что шаги вычли в обратном порядке
    # (четвёртая ловушка, docs/DOMAIN.md §6).
    "tp": (0.0, 1.0),
}

# Базовый шаг авторегрессии — по-прежнему 6 часов: только последний подшаг
# возвращается в модель на вход. 10 суток = 40 шагов.
STEP_HOURS: Final = 6
FORECAST_STEPS: Final = 40
FORECAST_HOURS: Final = STEP_HOURS * FORECAST_STEPS

# Aurora 1.5 умеет промежуточные сроки: срок передаётся модели параметром
# (`fine_lead_times`), а не набирается шагами (ADDENDUM-01 §1). Часовой слой
# кладём на первые трое суток — дальше он стоит дороже, чем стоит
# (ADDENDUM-01 §3: 240 сроков × 91 поле = 90 ГБ, не влезает).
FINE_STEP_HOURS: Final = 1
FINE_HORIZON_HOURS: Final = 72

#: Допустимые значения `step_hours` в запросе к API (docs/API_CONTRACT.md §2).
#: Валидатор им не пользуется: он проверяет шаг того слоя, в который пишут,
#: а не «один из разрешённых» — набор с шагом 1 ч, объявленный шестичасовым,
#: обязан быть отвергнут.
STEP_HOURS_ALLOWED: Final = (FINE_STEP_HOURS, STEP_HOURS)

# Часовой слой хранит не все 90 полей, а восемь: 72 срока × 90 полей — это
# 22 ГБ на прогон, весь бюджет ADDENDUM-01 §3 целиком. Восемь укладываются
# в 2.4 ГБ. Список согласован с фронтендом (решение от 2026-08-02) и
# заморожен: добавить переменную позже — значит переписать раскладку Zarr,
# потому что часовой слой лежит одним массивом по оси переменных.
HOURLY_VARS: Final = ("2t", "10u", "10v", "msl", "tp_1h", "tcc", "2d", "i10fg")


class Layer(NamedTuple):
    """Слой хранилища: набор полей плюс шаг по времени.

    Слоёв три, и различаются они не только шагом: у часового нет полей на
    уровнях давления вовсе, а у анализа приземных полей 18, а не 25. Валидатор,
    который знает только один набор, часовой слой отвергнет как неполный —
    поэтому набор передаётся ему явно, а не берётся по умолчанию.
    """

    name: str
    surface_vars: tuple[str, ...]
    atmos_vars: tuple[str, ...]
    step_hours: int
    steps: int

    @property
    def fields_per_step(self) -> int:
        return len(self.surface_vars) + len(self.atmos_vars) * len(PRESSURE_LEVELS)

    @property
    def fields(self) -> int:
        return self.fields_per_step * self.steps


LAYERS: Final[Mapping[str, Layer]] = MappingProxyType(
    {
        # 10 суток по 6 ч: основной слой, 90 полей на срок.
        "coarse": Layer("coarse", SURFACE_STORED_VARS, ATMOS_VARS, STEP_HOURS, FORECAST_STEPS),
        # Трое суток по часу: восемь приземных полей, уровней давления нет.
        "hourly": Layer("hourly", HOURLY_VARS, (), FINE_STEP_HOURS, FINE_HORIZON_HOURS),
        # Вход инференса: два шага анализа, 18 приземных полей (ADDENDUM-01 §1).
        "analysis": Layer("analysis", SURFACE_INGESTED_VARS, ATMOS_VARS, STEP_HOURS, 2),
    }
)

# Допустимые значения поля `source` в ответе API — docs/DATA_CONTRACT.md §3.
SOURCES: Final = (
    "aurora-forecast",
    "ifs-analysis",
    "gfs-analysis",
    "era5t",
    "era5-final",
    "climatology",
)

# Первый год ERA5. Запрос за более раннюю дату — 404, а не пустой ответ.
HISTORY_START_YEAR: Final = 1940
