# То, чего хватает для `make test` на ноутбуке: валидаторы, хранилище и API
# тестируются на фикстурах и синтетике, без cfgrib и без сети.
# Отдельный файл потому, что eccodes на Apple Silicon ставится из conda-forge,
# а не из pip (docs/SETUP.md §4) — и не должен блокировать запуск тестов.

xarray>=2025.1
numpy>=1.26
zarr>=3.0
# Без dask редукции xarray читают поле целиком: валидатор, проверяющий срез
# на 11.5 ГБ, обязан считать по чанкам. Это проверяется тестом на память,
# поэтому dask нужен и в минимальном окружении.
dask[array]>=2025.1

# Полоса 1 (`/v1`): приложение и тестовый клиент. Модель и адаптеры сюда не
# приходят — API только читает Zarr (`tests/test_boundaries.py`).
fastapi>=0.115
httpx>=0.28
# `make serve` и `make demo` должны работать после обычного `make venv`,
# а не только в полном service-профиле.
uvicorn>=0.34

pytest>=8.3
ruff>=0.9
mypy>=1.14
